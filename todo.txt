 - Raise a diagnostic if DependsOn exists, and no OnPropertyChanged can be generated
 - OnAnyPropertyChanged book, again raise if no OnPropertyChanged can be generated
 - Raise if DependsOn used on a property which isn't Notify?
